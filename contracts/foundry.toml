[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc_version = "0.8.28"
optimizer = true
optimizer_runs = 800
evm_version = "paris"
fs_permissions = [{ access = "read", path = "./" }]

[rpc_endpoints]
fork = "${FORK_RPC_URL}"
